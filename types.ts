export interface categoryDataType {
  id: string;
  name: string;
  icon: string;
}

export type InputIdTypes =
  | "name"
  | "title"
  | "description"
  | "targetAmount"
  | "targetDate"
  | "imageURL";
